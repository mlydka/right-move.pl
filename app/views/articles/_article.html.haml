.article
  %h2= link_to article.title, article_path(article), {:class => 'h2', :title => article.title}
  %p= article.introduction

  %p.more
    &hellip;
    = link_to 'więcej', article_path(article), {:title => 'Zobacz artykuł'}

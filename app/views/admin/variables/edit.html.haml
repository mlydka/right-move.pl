%h3.header Edytuj Zmienną

.form
  - form_for @variable, :url => admin_variable_path(@variable) do |form|
    = render :partial => 'form', :object => form

    %p
      = link_to "Anuluj", admin_variables_path
      = submit_tag "Zmień"
